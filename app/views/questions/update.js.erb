$('.question').html('<%= j render @question, locals: {question: @question} %>');
$('.edit-question-link').click(function(e) {
  e.preventDefault();
  $(this).hide();
  $('form#edit-question').show();
});